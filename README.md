# JavaTrivia
A simple trivia web application built with Java Spring boot

In the backend directory, run `gradlew bootRun`.
In the frontend directory, run `npm run dev`.
Then visit http://localhost:3000.
